export default function Startseite() {
    return (
        <div>
            <h1>Successfully logged in!</h1>
            {/* {users.map((user: any) => (
            <div key={user.id}>
                <h1>{user.firstName}</h1>
            </div>
            ))} */}
        </div>
    )
}